# Contact — Backend

`/api/contact-submissions/` — POST endpoint that validates input (Zod) and writes a `ContactSubmission` row.

Admin review happens through `/api/admin/contact-submissions/` (in [admin](../admin/)).

> _TODO: read handler and document exact validation rules + response shape._
