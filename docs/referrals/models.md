# Referrals — Models

## `ReferralEvent`

[src/models/ReferralEvent.ts](../../src/models/ReferralEvent.ts) — append-only event log of referral actions.

> _TODO: pull schema (fields, indexes, event types)._
