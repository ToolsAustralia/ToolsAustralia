# Referrals — Frontend

## Hook

| Hook | Purpose | Source |
|---|---|---|
| `useReferralCode()` | Resolve referral code from URL/session | [src/hooks/useReferralCode.ts](../../src/hooks/useReferralCode.ts) |

## Where it's consumed

Referral codes are read on signup pages (members can be attributed at registration) and on certain promo landing pages.

> _TODO: enumerate exact pages once root docs are merged._
