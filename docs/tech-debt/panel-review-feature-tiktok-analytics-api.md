# Panel review — TikTok integration (feature/tiktok-analytics-api)

- **Branch:** `feature/tiktok-analytics-api` @ `80e92667` — **clean vs `origin/main`** (subsystem review of the merged TikTok integration, NOT a diff review; no PR exists).
- **Date:** 2026-07-24
- **Base ref / scope:** current state of the TikTok integration file set (~40 files: tracking pixel/CAPI/dispatch, attribution, Marketing sync, cron, admin API + UI, Norm mirror). Findings anchored to SHA `80e92667` — re-grep `file:line` before fixing; lines rot.
- **Acceptance spec:** the user's stated criteria (no GitHub ticket): browser + server tracking firing, conversions/purchases integrated, data accurate the moment the new Marketing token lands. Verdicts: R1 PARTIAL · R2 MET · R3 MET · **R4 MISSING** · R5 PARTIAL. **Ship verdict: conversion side ready; token-day NOT ready until F-001 + F-004.**
- **Domain Manifest domains touched by findings:** tracking, admin, metrics-analytics, infrastructure, shared-ui.
- **Artifact:** https://claude.ai/code/artifact/3e6da79c-1d7d-4584-a13a-dd98f8dadb57
- **Gate:** type-check ✅ clean (first run raced the e2e-server boot rewriting next-env.d.ts — rerun clean); lint ✅ = the 6 known baseline errors only (e2e/fixtures/test.ts ×3, scripts/codemod-dark-text.js ×2, scripts/migrate-klaviyo-draw-properties.ts ×1). Tests: 15/15 targeted tsx suites pass; `test:dashboard-stats-aggregator` + hourly-revenue-reconciliation SKIPPED (connect to dev DB — review safety rule).
- **Evidence:** 6 parallel reviewers + 2 adversarial skeptics per P0/P1 (54 agents). UI rendered against the seeded e2e env (localhost:3799), Playwright + sharp, measured (not source-derived) contrast/geometry; 7 webp captures embedded in the Artifact (source PNGs in the session scratchpad — ephemeral). Live probes this session: TikTok Events API accepted a test event (HTTP 200 · code 0 · request_id 20260724134703FE93D04174DDE40324BD); all Marketing endpoints 40001 with current token; prod `tiktokadinsightsdailies` = 0 docs; prod cron 500 nightly since 2026-07-16. No video evidence produced (no @demo spec covers these admin surfaces).

## Handoff

Fresh session? Run `/panel-fix` on this branch, or paste:

> Read `docs/tech-debt/panel-review-feature-tiktok-analytics-api.md`. Only F-014 and F-024 remain, both deliberately parked — read their notes before touching either.
> Findings were written against `80e92667`; the 2026-07-24 Now-batch fixes sit uncommitted on top — re-grep each `file:line`, they may have moved.
> One commit-worthy change per finding. Do NOT commit. When a finding is done, tick its box
> and fill `_Handled:_` with the date. If a fix turns out to be wrong, mark it Overridden with
> a reason instead of silently skipping it.

**Now (do these):** _nothing outstanding_ — all 21 actionable findings are fixed (see ticked boxes). The four below are deliberately parked, each for a stated reason.
**Next:** _nothing_ — F-019 and F-021 were resolved 2026-07-24 (product decision + feature build respectively).
**Later:** **F-014** — its precondition (a successful live sync proving the response shapes) is **now met** as of 2026-07-29, and a third duplicate of the fetch loop has appeared in `TikTokAdDestinationService`; open only because nobody has asked for the refactor · **F-024** accepted repo-wide trade-off (Meta behaves identically), document-only. **23 of 25 findings closed.**

---

## ⚠️ Production deployment prerequisites (2026-07-29) — read before merging

The branch platform-scopes two shared collections. **Both migrations must run in production BEFORE the first TikTok sync writes there**, in this order:

```bash
npx tsx scripts/migrations/2026-07-29-platform-scope-ad-destinations.ts            # dry-run first
npx tsx scripts/migrations/2026-07-29-platform-scope-ad-destinations.ts --live
npx tsx scripts/migrations/2026-07-29-platform-scope-landing-page-metrics.ts
npx tsx scripts/migrations/2026-07-29-platform-scope-landing-page-metrics.ts --live
```

Why the order and timing matter: `SpendByUrlAggregationService.recomputeForDateRange` deletes by `{platform, adAccountId, date}` and `landingpagemetricsdailies` has **no TTL**. Rows left unstamped are invisible to that scoped delete but still occupy the unique index, so the rebuild collides instead of replacing — and `metaaddestinations`'s old globally-unique `adId_1` would reject TikTok ads that happen to share a numeric id with a Meta ad. Both scripts are idempotent, dry-run by default, and abort on duplicate keys. Applied to dev 2026-07-29 (3,078 + 834 docs).

After deploying, verify with `npm run verify:tiktok-accuracy` (13 checks; see docs/metrics-analytics/testing.md). `/api/cron/sync-tiktok-ads` now runs the FULL pipeline (insights → destinations → rollup), so the first prod run also backfills TikTok's rollup for its trailing 8-day window; older history needs `npm run seed:tiktok-insights -- --days=60` then `npm run sync:tiktok-spend-by-url -- --since=… --until=…`.

_2026-07-24: THREE batches fixed in the working tree (uncommitted) — batch 1 (F-001..F-004, F-011), batch 2 (F-005, F-007, F-008, F-010, F-012), batch 3 (F-006, F-009, F-013, F-015..F-018, F-020, F-022, F-023, F-025). **21 of 25 findings closed; the 4 open ones are parked by decision, not by omission.** Context a fresh session needs: `fetchTikTokAdInsightsDaily` THROWS `TikTokReportError` on API failure (null = unconfigured ONLY) and treats an unparseable body as failure; the cron records a `TikTokSyncRun` status doc every run, warns on `metric-names-suspect`, and checks the account's currency/timezone; both Marketing fetchers and the Events sender route through src/lib/http/outbound.ts; `npm run test:tiktok-sync-contract` + `npm run test:tiktok-capi-guards` pin the failure-visibility contracts — do not soften them._

## Findings

### P0

- [x] **F-001** · P0 · Arch · `src/services/admin/dashboard-stats/adChannelProviders.ts:102` — When the new TikTok token goes live, the Overview Advertising card keeps saying "Awaiting sync" forever, and blended ROAS/MER quietly ignore TikTok spend.
      _What:_ The card/MER read snapshot `adChannels["tiktok"]` (PLATFORM_TO_AD_CHANNEL_KEY maps it), but `AD_CHANNEL_PROVIDERS = [facebookAdChannelProvider]` only — nothing ever writes a tiktok entry. TikTok tab populates from the cron; the flagship Overview never does. Raised by ALL SIX reviewers.
      _Fix:_ Add `tiktokAdChannelProvider` (key "tiktok") to adChannelProviders.ts: aggregate that AEST day's TikTokAdInsightsDaily (sum spendCents/revenueCents/conversions, roas = revenue/spend); `{status:"error"}` when `isTikTokAdInsightsConfigured()` false OR read throws (preserve-prior rule — June Meta wipe precedent), `{status:"empty"}` on zero rows; append to AD_CHANNEL_PROVIDERS; update docs/admin/. This provider also feeds the planned platform-ROAS column.  _Shot:_ artifact §01  _Handled:_ 2026-07-24, working tree (uncommitted) — provider reads synced Mongo rows (no live API call); registered alongside facebook; docs/admin/backend.md + mer-table.md + docs/tracking/models.md updated; `test:merge-ad-channels` + `test:mer` + `test:advertising-card-model` pass

### P1

- [x] **F-002** · P1 · UX/Truth · `src/components/admin/TikTokAdBreakdownTable.tsx:57-63` + `src/app/admin/component/overview/sections/AdvertisingPlatformCard.tsx:139-141` — Admin surfaces show calm "awaiting / no data yet" while the sync has been crashing nightly for 8 days.
      _What:_ Configured-but-failing is rendered identically to not-configured/no-data on every TikTok surface; a dead token post-launch looks like a quiet ad account. Also surfaces raw env-var names to staff (TikTokAdBreakdownTable.tsx:61).
      _Fix:_ Cron upserts a single sync-status doc (outcome ok/error + TikTok code/message + finishedAt) every run; `/api/admin/tiktok-ads/insights` returns it as `syncHealth`; empty states render "TikTok sync failing since 16 Jul — permission error (40001)" vs "Synced 02:45 — no spend recorded". Replace env-var copy with "TikTok Marketing API isn't connected yet".  _Shot:_ artifact §03  _Handled:_ 2026-07-24, working tree (uncommitted) — new `TikTokSyncRun` model (manifest: tracking) + `tiktokSyncStatus.ts` service; `fetchTikTokAdInsightsDaily` now throws `TikTokReportError` (null = unconfigured only); cron records ok/error every run; route composes `syncHealth` (Norm schema untouched — verified no-drift); table renders failing/synced/not-connected states + stale-data banner; card shows red "Sync failing" over the amber awaiting label. Error chain live-verified: seed dry-run surfaced the real 40001 message end-to-end

- [x] **F-003** · P1 · Eng · `src/services/admin/hourlyRevenueByPlatform.ts:87-93` — Combined ad-channels hourly spend silently shows Meta-only totals when TikTok's configured fetch fails (production's live state today).
      _What:_ `fetchTikTokHourlySpend` nulls on both unconfigured AND error; group summer skips null sources, so meta-success + tiktok-failure = Meta-only sum displayed as the group total with no marker.
      _Fix:_ In computeGroupHourlySpend: if a key is configured (`isTikTokSpendConfigured()` / Meta env check) but its fetch returned null, return null for the whole group → UI renders "—".  _Handled:_ 2026-07-24, working tree (uncommitted) — computeGroupHourlySpend returns null for the whole group when a CONFIGURED source (Meta env check / isTikTokSpendConfigured) fetch-fails; docs/client-state/patterns.md updated

- [x] **F-004** · P1 · Ops · `src/app/api/cron/sync-tiktok-ads/route.ts:44-49` — Spend older than 8 days can never be synced; the broken-token weeks are unrecoverable and token day starts with an empty table.
      _What:_ Cron hard-codes subDays(7) trailing window; sole caller of syncDateRange; no TikTok analogue of `scripts/seed-meta-insights.ts`.
      _Fix:_ Create `scripts/seed-tiktok-insights.ts` cloning seed-meta-insights.ts (--days=N default 30, --dry-run default-safe, progress lines per ops-script rule); add `seed:tiktok-insights` to package.json; run `--days=60` on token day.  _Handled:_ 2026-07-24, working tree (uncommitted) — scripts/seed-tiktok-insights.ts (live path delegates to TikTokInsightsSyncService.syncDateRange; dry-run fetches + prints live raw metric keys for the F-005 check); seed:tiktok-insights(+:dry) in package.json; dry-run exercised against the live API (exits 1 with the real 40001 — correct until the token is fixed); run --days=60 on token day

- [x] **F-005** · P1 · QA/Arch · `src/services/admin/tiktok/tiktokAdInsights.ts:103-114,149-156` — If TikTok's real metric names differ from the guesses, the first sync stores zero conversions/revenue for every ad with no failure signal.
      _What:_ Report API returns only requested metrics, so parseMetric's fallback keys are unreachable; wrong guess = confident zeros. raw is stored but nothing prompts a look.
      _Fix:_ Token-day: read live metric names off a stored `raw` row before trusting week one. Code: after sync, if Σclicks>0 && Σconversions==0 && ΣrevenueCents==0, add `warning:"metric-names-suspect"` to cron JSON + console.error.  _Handled:_ 2026-07-24, working tree (uncommitted) — syncDateRange now returns window `totals` (clicks/conversions/revenueCents) + exported pure predicate `metricNamesSuspect()`; cron logs WARNING and returns `warning: "metric-names-suspect"`, seed script prints the same; pinned by test:tiktok-sync-contract

- [x] **F-007** · P1 · Arch · `tiktokAdInsights.ts:120-122` + `tiktokHourlySpend.ts:53-55` + `hourlyRevenueByPlatform.ts:83-90` — A slow TikTok API can hang the admin hourly-revenue screen; request-path fetches have no timeout and run sequentially after Meta.
      _Fix:_ `signal: AbortSignal.timeout(8000)` on both TikTok fetches; Promise.all the Meta+TikTok hourly fetches in computeGroupHourlySpend.  _Handled:_ 2026-07-24, working tree (uncommitted) — `signal: AbortSignal.timeout(8000)` on both TikTok report fetches AND migrated both to `outboundFetch` (src/lib/http/outbound.ts — the keep-alive-bounded dispatcher every third-party integration should use); computeGroupHourlySpend now fetches Meta+TikTok via Promise.all (was sequential), preserving the F-003 configured-but-failed rule. NOTE: the panel claimed "Meta bounds its calls" — VERIFIED FALSE, src/lib/facebook-marketing.ts has no timeout either; TikTok is now the stricter of the two (Meta timeout left out of scope)

- [x] **F-008** · P1 · QA · `src/services/admin/tiktok/TikTokInsightsSyncService.ts:34-45` — Nothing pins the throw-on-configured-failure contract; one careless edit turns the cron green-while-broken (June Meta wipe class).
      _Fix:_ Add `test:tiktok-sync-contract` (tsx, zero-DB): stub fetch → null with env configured ⇒ assert syncDateRange throws; env absent ⇒ clean {configured:false}. Wire into package.json.  _Handled:_ 2026-07-24, working tree (uncommitted) — src/services/admin/tiktok/__tests__/tiktokSyncContract.test.ts + test:tiktok-sync-contract in package.json; zero-DB/zero-network (stubs globalThis.fetch); 5/5 pass, pins unconfigured-noop / throws-on-40001 / throws-on-network-error / empty-is-success / metricNamesSuspect

- [x] **F-011** · P1 · Data-accuracy · `src/app/admin/component/AllPlatformsManagement.tsx:114` — All-Platforms drill-down popover/modal shows "today" numbers regardless of the selected date range.
      _What:_ `<AdvertisingPlatformCard stats={stats} loading={statsLoading} />` omits dateRange/startDate/endDate → card defaults to "today" for its drill-down fetches while the surrounding tab shows the selected range. Live bug, token-independent. Consolidator re-verified the line.
      _Fix:_ Pass `dateRange={df.dateRange} startDate={df.startDate} endDate={df.endDate}` (same props the Overview passes).  _Handled:_ 2026-07-24, working tree (uncommitted) — AllPlatformsManagement now passes dateRange/startDate/endDate from its useAdminDateFilter into AdvertisingPlatformCard (same props the Overview passes)

- [x] **F-012** · P1 · Privacy · `src/app/(site)/privacy/page.tsx:292` — Privacy policy discloses the Facebook Pixel but never mentions TikTok, which tracks identically.
      _Fix:_ Add a TikTok bullet mirroring the Facebook wording (pixel + server Events API, hashed identifiers, TikTok privacy-policy link). NOTE: trips the CUSTOMER.md hook — update CUSTOMER.md's third-party-data section in the same change (genuine fact change).  _Handled:_ 2026-07-24, working tree (uncommitted) — privacy §7 now names TikTok Pixel in Marketing Cookies + the third-party list, and discloses server-side conversion sharing to Meta and TikTok with hashed identifiers; CUSTOMER.md §8e records the disclosure change; docs/tracking/rules.md R4 gains a disclose-every-provider corollary

### P2

- [x] **F-006** · P2 · Arch · `tiktokAdInsights.ts:146` — AUD/Sydney assumptions never verified against the live account (downgraded from P1: AU business ⇒ AUD near-certain). _Fix:_ Token-day: GET /advertiser/info/, assert currency==AUD && timezone≈Sydney; console.error loudly otherwise.  _Handled:_ 2026-07-24, working tree (uncommitted) — AUTOMATED rather than left as a manual step: `checkTikTokAccountAssumptions()` + `describeAccountAssumptionMismatch()` in tiktokAdInsights.ts GET /advertiser/info/ and compare currency (must be AUD) + reporting timezone (must look like Australia/Sydney). Wired into seed:tiktok-insights:dry (token-day pre-flight) AND the nightly cron post-sync (best-effort, returns `assumptionsWarning`, never fails the sync)
- [x] **F-009** · P2 · QA · `src/lib/tiktok.ts:152-206` — sendTikTokEvent's three refusal guards untested (Facebook's are). _Fix:_ Extend test:tiktok-capi: non-prod-without-test-code→false; missing event_id→false; 200-with-code≠0→false.  _Handled:_ 2026-07-24, working tree (uncommitted) — new test:tiktok-capi-guards (10 assertions, zero-network, stubs globalThis.fetch): missing pixel id / missing token / non-prod-without-test-code / blank+whitespace+undefined event_id / 200-with-code-40001 / unparseable body / transport error returns false / prod omits test code / prod WITH TIKTOK_USE_TEST_EVENTS forwards it. Documented in docs/tracking/testing.md
- [x] **F-010** · P2 · A11y · `AdvertisingPlatformCard.tsx:50` — Amber "Awaiting sync"/"Needs spend" measured 2.51:1 in light mode (AA needs 4.5:1; dark passes 5.74:1). _Fix:_ `text-amber-700 dark:text-amber-500/80` (measured 5.02:1).  _Handled:_ 2026-07-24, working tree (uncommitted) — AWAITING token → text-amber-700 in light (5.02:1, was 2.51:1); same token fixed in MerByDrawCard.tsx which shared the failing style; dark mode unchanged (already 5.74:1)
- [x] **F-013** · P2 · Eng · `tiktokAdInsights.ts:123-124` (+hourly twin) — HTTP 200 with malformed body counts as success → "successful" 0-row sync. _Fix:_ `body.code !== 0` strict + treat unparseable body as error, both fetchers.  _Handled:_ 2026-07-24, working tree (uncommitted) — both fetchers now `res.json().catch(() => null)` + `!res.ok || !body || body.code !== 0`; error text distinguishes 'unparseable response body' from a real TikTok code. An HTTP 200 gateway error page can no longer be read as code 0 (ad-insights: was a 'successful' 0-row sync; hourly: was an all-zeros spend array asserting $0 instead of '—')
- [ ] **F-014** · P2 · Eng · `tiktokHourlySpend.ts:29-31` — Paginated fetch loop + configured-check duplicated under two names. _Fix:_ When next touched, extract shared fetchTikTokReport(params); single isTikTokMarketingConfigured name.  _Handled:_ — **PRECONDITION NOW MET (2026-07-29):** it was deferred "until the first successful live sync proves the response shapes", and the token has since gone live — many successful syncs, response shapes confirmed, and a third caller has appeared (`TikTokAdDestinationService` has its own `fetchAllPages`). Still open only because nobody has asked for the refactor; it is now unblocked rather than waiting on anything.
- [x] **F-015** · P2 · Docs · `src/lib/tracking/providers/tiktok.ts:9-12` + CLAUDE.md manifest — References to deleted `src/components/TikTokPixel.tsx`; manifest tracking domain lists the ghost path. _Fix:_ Fix comment; remove ghost glob from tracking domain paths.  _Handled:_ 2026-07-24, working tree (uncommitted) — providers/tiktok.ts comment rewritten (it is now the SOLE Window.ttq augmentation; the TS2687/TS2717 duplicate-declaration constraint no longer exists) and the ghost `src/components/TikTokPixel.tsx` glob removed from the tracking domain in the CLAUDE.md manifest. Other repo mentions checked — all correct past-tense history
- [x] **F-016** · P2 · UX · `AdvertisingPlatformCard.tsx:209` — Hover popover clips at viewport bottom. _Fix:_ If r.bottom+popoverH>innerHeight, position above the row.  _Shot:_ artifact §08  _Handled:_ 2026-07-24, working tree (uncommitted) — popover flips ABOVE the row when it would not fit below, clamps to an 8px top margin, and clamps `left` inside the right edge; box gains maxHeight + overflowY:auto. POPOVER_WIDTH/POPOVER_MAX_HEIGHT shared so fit math and rendered box cannot drift
- [x] **F-017** · P2 · A11y · `src/components/admin/ui/DataTable.tsx:45-49` — Clickable rows keyboard-unreachable. _Fix:_ When onRowClick set: tabIndex=0, role="button", Enter/Space keydown.  _Handled:_ 2026-07-24, working tree (uncommitted) — fixed in the shared DataTable so EVERY consumer inherits it: when onRowClick is supplied rows get tabIndex=0, role='button', Enter/Space onKeyDown (Space preventDefault so it activates rather than scrolls) and a focus-visible ring; non-interactive tables stay out of the tab order. docs/admin/frontend.md's 'known kit limitation' note struck through and updated
- [x] **F-018** · P2 · Copy · `src/app/admin/component/AdminPage.tsx:159-161` — Header renders "Tiktok Ads" (slug+capitalize) vs sidebar "TikTok Ads". _Fix:_ Label map special-case "tiktok-ads" → "TikTok Ads".  _Shot:_ artifact §08  _Handled:_ 2026-07-24, working tree (uncommitted) — new `adminTabLabel(tabId)` in adminTabs.ts returns the sidebar's own label; AdminPage header uses it and drops the `capitalize` class. Root cause was deriving a brand name from a slug — now one source of truth (global CLAUDE.md naming rule)
- [x] **F-019** · P2 · Product · `src/components/modals/UnifiedModalManager.tsx:216` — Cookie-consent modal can never appear; Decline wouldn't stop tracking. _Fix:_ Needs product decision: wire consent gating for pixels, or delete the dead modal. Needs investigation before code.  _Handled:_ 2026-07-24, working tree (uncommitted) — RESOLVED BY PRODUCT DECISION (DJ): no consent banner; every visitor gets the pixels. Deleted PixelConsentModal.tsx + its UnifiedModalManager case, the "pixel-consent" ModalType/priority entry, and the dev-gallery entry. Decision + what a real consent gate would require documented as docs/tracking/rules.md R9, cross-referenced from CUSTOMER.md §8e, shared-ui/frontend.md, client-state/patterns.md, dev-tooling/frontend.md. Note: hasPixelConsent() already hard-returned true ("auto-accept mode"), so this changed NO runtime behavior — it removed a control that never worked
- [x] **F-020** · P2 · Eng · `src/utils/tracking/pixel-purchase-tracking.ts:923-924` — Renewal-tracking call claims to send renewals to TikTok but no-ops; comment stale. _Fix:_ Delete the dead call + comment (renewals deliberately excluded from acquisition tracking).  _Handled:_ 2026-07-24, working tree (uncommitted) — VERIFIED the no-op claim first (trackPixelSubscriptionRenewal runs server-side from the Stripe webhook; trackTikTokEvent returns immediately when `window` is undefined). Deleting just the call orphaned the whole body, so the function is now an explicit documented no-op with the misleading 'Only tracks to TikTok/Klaviyo' docstring corrected. Function + webhook call KEPT deliberately — removing them means editing the Stripe webhook handler (payments risk path) for zero behavioral gain
- [x] **F-021** · P2 · Data · `src/models/User.ts:259-275` — signupAttribution stores no ttclid/fbclid → per-platform signup counts are UTM-confidence only. _Fix:_ Capture ttclid/fbclid into buildSignupAttribution if click-verified signup attribution is wanted (prereq for upgrading the planned signups-per-platform feature).  _Handled:_ 2026-07-24, working tree (uncommitted) — BUILT, as the prerequisite for the signups-per-platform feature DJ requested. Improved on the finding: stores the RESOLVED platform (signupAttribution.clickPlatform) rather than the raw ttclid/fbclid, so signup analytics get click-verified confidence with NO new identifier on the customer record. Resolved via extractClickIdsFromRequest (same extractor the payment path uses; most-recent capture wins) and threaded into ALL FOUR register branches; wrapped in try/catch so attribution can never break registration. CUSTOMER.md + docs/auth/backend.md + docs/subscription/models.md updated
- [x] **F-022** · P2 · Tracking · `src/lib/tracking/providers/tiktok.ts:169` — Only the landing page fires a TikTok page view; SPA navigations invisible. _Fix:_ Fire ttq.page() on route change in the existing per-route tracking hook.  _Handled:_ 2026-07-24, working tree (uncommitted) — root cause was subtler than the finding: SPA routes DID reach TikTok, but as `ttq.track("PageView")`, which registers a CUSTOM event, not TikTok's standard page view. tiktokProvider.pixelTrack now translates canonical PageView → `ttq.page()`. Put in the provider (not ConversionPixels) so the generic component stays provider-agnostic and nothing double-fires
- [x] **F-023** · P2 · Perf · `src/services/admin/tiktok/tiktokAdInsightsQuery.ts:57` — Unprojected .find() ships every row's raw API blob to the aggregation. _Fix:_ `.select("-raw")` (repo footgun rule #3).  _Handled:_ 2026-07-24, working tree (uncommitted) — explicit `.select()` include-list excluding `raw` on the per-ad read (repo footgun rule #3)
- [ ] **F-024** · P2 · Accepted · `src/models/TikTokAdInsightsDaily.ts:61-66` — 60-day TTL erodes >60d history for inactive ads; same accepted pattern as MetaAdInsightsDaily. _Fix:_ Accepted repo-wide trade-off — document in docs/admin/; revisit only if >60-day range accuracy becomes a requirement.  _Handled:_ —

### Added during /panel-fix (not from the original panel)

- [x] **F-025** · P2 · Arch · `src/lib/tiktok.ts:175` — The TikTok Events API sender calls raw `fetch`, so it misses the keep-alive fix that exists precisely for this failure and can die with an opaque "fetch failed" on a warm Vercel invocation.
      _What:_ `src/lib/http/outbound.ts` documents that new third-party integrations SHOULD route through `outboundFetch` — its bounded keep-alive dispatcher is the fix for the `UND_ERR_SOCKET` production incident (a frozen serverless function reusing a socket the remote already closed). `sendTikTokEvent` still uses global `fetch`, so a dropped conversion event surfaces only as `[TikTok CAPI] Network error … fetch failed`. Found while fixing F-007 (which scoped only the two Marketing-API fetchers, now migrated). NOT drive-by fixed: `src/lib/tiktok.ts` is in `BUSINESS_TRIGGER_GLOBS`, so editing it forces a README/BUSINESS.md touch — deliberately left for its own change.
      _Fix:_ In `src/lib/tiktok.ts`, import `outboundFetch` from `@/lib/http/outbound` and use it at `:175` in place of `fetch`; log `describeFetchError(err)` in the existing network-error catch so the undici cause code is visible. Check `src/lib/facebook.ts` in the same pass — if the Meta CAPI sender also uses raw `fetch`, fix both together (one BUSINESS.md touch covers it).  _Handled:_ 2026-07-24, working tree (uncommitted) — sendTikTokEvent now uses `resilientFetch` (keep-alive-bounded dispatcher + bounded retry, 5s/attempt, 2 retries — safe because TikTok dedups on event_id) and logs describeFetchError's undici cause code. Matches src/lib/facebook.ts exactly. BUSINESS.md touched (rule 5: src/lib/tiktok.ts is a BUSINESS_TRIGGER_GLOB)

## Corrections recorded by verification

1. Assistant's earlier session claim "spend flows to the dashboard with zero code changes once the token works" — **false for the Overview card / blended ROAS / MER** (true for the TikTok tab + hourly). F-001 is the gap.
2. "8-day window off-by-one" — **refuted**: subDays(7) with inclusive Sydney-formatted bounds = exactly 8 days; doc comment correct.
3. Currency-assumption P1 → **P2**: AU business on AU ad account ⇒ AUD near-certain; token-day assert only (F-006).
4. Browser-pixel live check **UNVERIFIABLE in e2e env by design** (e2e/lib/env.ts:77 blanks NEXT_PUBLIC_TIKTOK_PIXEL_ID); server-side send live-proven instead; browser/server event_id dedup parity verified by code trace (Purchase keyed by paymentIntentId both sides).

## Feature feasibility (verified, requested this session)

- **Signup count per platform: FEASIBLE (M).** `User.signupAttribution` (UTM snapshot, register/route.ts:487) + `normalizeUtmToPlatform()` → aggregate by createdAt range; add `signups` to attributedRevenue entries; render under conversions. UTM-confidence only until F-021.
- **Dual ROAS (server-final + platform-reported): FEASIBLE (S–M).** Platform revenue/roas already captured per channel in snapshot `adChannels` (Meta fills today; DashboardStatsService.ts:261 reads only .spend). Expose platformRevenue/platformRoas per platform; second RoasState in advertisingCardModel; two columns on the card. TikTok values arrive via F-001's provider.
