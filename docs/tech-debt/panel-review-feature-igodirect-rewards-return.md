# Panel review — feature/igodirect-rewards-return (rewards-return Phase 1)

- **Branch:** `feature/igodirect-rewards-return` · reviewed **2026-07-24** against `origin/main` (`git diff origin/main` — working tree, **uncommitted at review time**; line numbers will move once committed — re-grep each `file:line`).
- **PR:** none yet. **Acceptance:** graded against the session-approved Phase 1 plan (12 criteria) — **12/12 MET**; two deviations judged justified (SSO button flag-gated in the covered state; extra guest unlock CTA). No spec was invented.
- **Domains touched:** partner, subscription, shared-ui, cart-shop-products, client-state, config-and-data, support-chat, infrastructure.
- **Artifact:** https://claude.ai/code/artifact/05b4c345-b2aa-47c3-a10b-1270e3d6aa09 (private by default — share from the page's Share menu).
- **Gates:** type-check ✓ · lint ✓ vs baseline (6 pre-existing errors on main; +2 warning drift proven inherited from main `b89a9e5b`) · `test:unlock-packages` ✓ · `test:chat-faqs` ✓ (72) · `test:member-level` ✓ · `test:member-status` ✓ · `build:partner-catalog` deterministic (byte-identical twice) · e2e suites NOT run (evidence server held port 3799) — see F-023.
- **Evidence:** seeded e2e server (`npm run e2e:env`), Playwright-measured captures at 390/1280, guest + seeded Tradie member, 16 states; crops in session scratchpad `reviewer-b/`; demo video `C:\Users\Genesis\Downloads\rewards-return-phase1-demo.mp4`. SSO flag was ON in the e2e env (prod stays dark). Unevidenced: purchase-success CTA render (needs real payment intent), past-due/paused banner states (no seeded user), real devices, emitted client-chunk contents.

## Handoff

Fresh session? Run `/panel-fix` on this branch, or paste:

> Read `docs/tech-debt/panel-review-feature-igodirect-rewards-return.md`. Fix ONLY the Now items: F-001, F-002, F-003, F-004, F-005.
> Findings were written against the UNCOMMITTED working tree of 2026-07-24 — re-grep each `file:line`, they may have moved.
> One commit-worthy change per finding. Do NOT commit. When a finding is done, tick its box
> and fill `_Handled:_` with the date. If a fix turns out to be wrong, mark it Overridden with
> a reason instead of silently skipping it.

**F-001…F-025: 24 verified genuinely closed** by the 2026-07-28 verification panel (pass 1 = 996557d0 · batch 2 = 7e584e6a · polish = 80548fcb · Later = 6e84bbfd · final = 78c8cbbe). **F-023 re-opened** — one third of its fix was never written.

**Now:** — round-2 Now batch DONE (F-027, F-028, F-029, F-030, F-031 + F-023 closed). Next up: F-032, F-033, F-034, F-035, F-050.
**Next:** F-032, F-033, F-034, F-035, F-050
**Later / own PR:** F-026 (focus visibility — site-wide CSS), F-037…F-049

_2026-07-28 panel-fix pass 5 (round-2 Now batch): **F-027, F-028, F-029, F-030, F-031 done** — and F-023 closed with them, since F-030 was its missing third part._

**F-027 was tuned against real measurements, not guessed twice.** The first attempt (mirroring the CTA column's meta line + link) OVERSHOT: measured skeleton 409px vs settled 337px, i.e. a 72px *upward* jump — because those two elements only render for an authed member short of the offer, and at first paint the account state is unknown. Removing them and adding `lg:` heights (the copy wraps on mobile but not desktop) landed at the achievable optimum:

| Viewport / state | before | after |
|---|---|---|
| 390 · guest + offer | 114.5px (member state) / 70px (guest) | **4px** |
| 390 · guest generic + XSS | 68.4px | **2px** |
| 1280 · all banner states | 3–48px | **10–11px** |
| control page (no banner) | 0px | **0px** |

The residual 10–11px at desktop is the **floor for a fixed-height skeleton**: the two desktop states settle 21px apart (262px vs 283px), so any single reservation is ≥10px wrong for one of them; 273px splits it. Mobile CLS distance-fraction drops from ~0.136 to ~0.005.

**Environment note that cost real time and proves F-034:** mid-measurement the harness reported "server ready" while a FOREIGN process held port 3799 — `EADDRINUSE` ×3 in `e2e-artifacts/logs/server.log` — and the first measurement run returned a uniform, plausible-looking `shift=0px PASS` for every state. Those numbers were void. Every figure above was re-taken after asserting `grep -c EADDRINUSE server.log` is 0. **Always run that check before trusting an e2e measurement until F-034 lands.**

Gates: type-check ✓ · `test:portal-return` ✓ (22 cases — added ambiguous-name, past-due-with-pack, login-hint-vs-session, and the `>=` equality boundary from F-036) · `test:unlock-packages` ✓ · `test:chat-faqs` ✓ · `test:sso-access` ✓ · lint **exactly** at the 6-error/33-warning baseline. F-036's boundary pin was folded in while editing the same test file (it was one assertion in the code path F-031 changed); it stays ticked in its own entry.


_2026-07-28 panel-fix pass 4 (Later batch): F-016–F-021, F-023–F-025 done. **F-016 landed the ladder in `partner-catalog-visibility.ts`** (the module that owns the percent concept) with `portal-return.ts` re-exporting and the build script importing it — verified the script still runs and emits byte-identical output. **F-023 cost the most:** the new `@smoke` spec appeared to fail hard (banner stuck in its skeleton for 60s), so it was investigated as a suspected regression from this batch. It is NOT: under identical fixture conditions (pinned `x-forwarded-for` + third-party route blocking) a warm server settles the banner in 3-8s with every `/api/*` 200 and zero page errors; a COLD Turbopack dev server can hold the skeleton past 60s because `/membership`'s client chunks compile on the first browser hit. Ruled out along the way: rate limiting (12 rapid `/api/major-draw` calls with the fixture IP → all 200), TanStack disabled-query semantics (repo is v5, so disabled queries do not pin `isLoading`), and a bundle-weight regression from F-016 (`partner-catalog-visibility` was already in the banner's import graph). Mitigations: a real-navigation `beforeAll` warm-up + a deterministic `aria-busy` settle helper; the characteristic is documented in the spec header and `docs/e2e/adding-a-spec.md`. Gates: type-check ✓, lint ✓ **exactly at the 6-error/33-warning baseline** (a 188-error run was traced to eslint walking the gitignored `e2e-artifacts/report/` bundle produced by these very e2e runs — not source), `test:portal-return` ✓, `test:unlock-packages` ✓ (now pins all 11 tiers), `test:chat-faqs` ✓, `test:member-level` ✓, `build:partner-catalog` ✓. e2e: all rewards-return specs pass (some need Playwright's retry on a cold server — see above)._

_2026-07-28 panel-fix pass 3 (polish): F-011–F-015 done. Verified: `test:portal-return` (18 cases incl. the new run-on-name pin) ✓, type-check ✓, targeted lint ✓, rule-11 grep over every changed string ✓ clean._

_2026-07-28 panel-fix pass 2: F-006–F-009 done in the working tree (uncommitted; F-010 was completed at commit time in pass 1's commit). Verified: `test:portal-return` (17 cases, updated for the allowlist + paused branch) ✓, `test:chat-faqs` ✓ (72), `build:chat-knowledge-pack` rebuilt ✓, type-check ✓, targeted lint ✓. F-008 note: `level` URL param is now fully ignored (allowlist supersedes it); FAQ 16's `/partner` link removal also fixed a latent bug (that page is the B2B application form, not a discount list)._

_2026-07-24 panel-fix pass 1: F-001–F-005 done in the working tree (uncommitted). Verified: type-check ✓, targeted lint ✓, `test:portal-return` (18 cases, new) ✓, `test:unlock-packages` ✓, `build:partner-catalog` via new prebuild wiring ✓, bundle boundary re-grepped (map imported only by the server page; util mentions it in a comment only) ✓, `@demo` e2e spec re-run green against the fixed banner ✓. **Correction recorded on F-001:** reviewer A's "CTA flickers between two labels" sub-claim was DISPROVED — `useMemberships` is fully static (`loading` hardcoded false), so the recommendation resolves on the banner's first render; no plans-loading gate was needed or added. **F-004 implemented as an additive login-hint line** (kept the approved sub copy) rather than reviewer C's sub-replacement._

## Findings

### Now

- [x] **F-001** · P1 · UI/Eng/Arch (3-way convergence) · `src/components/sections/membership/MembershipPortalReturnBanner.tsx:95` — Banner mounts 1.5–2.7s late and shifts the page 238–364px; ~~CTA label flickers~~ (flicker sub-claim DISPROVED — useMemberships is static/synchronous; no plans gate needed).
      _What:_ `if (!portalReturn || isLoading) return null;` gates the whole section on client queries although `portalReturn` is server-resolved; `useCurrentMajorDraw` has no guest gate so even guests wait. Once mounted, the CTA renders the scroll fallback then swaps to "Unlock with the {plan}" when `useMemberships` settles.
      _Fix:_ split the guard: `if (!portalReturn) return null;` then while `isLoading` render the same `<section>` shell (identical bg/padding classes) with the "Partner catalogue" badge row + skeletons (`motion-safe:animate-pulse`, `aria-busy`). Swap copy in place; never unmount.  _Shot:_ scratchpad `reviewer-b/ev-cls-late-mount-390.webp`  _Handled:_ 2026-07-24, working tree (uncommitted)
- [x] **F-002** · P1 · Eng/QA/Arch (3-way convergence) · `package.json:25-26` — CSV can be edited without regenerating the two generated catalog files; nothing fails.
      _What:_ `build:partner-catalog` is manual-only, unlike all four sibling generated artifacts (incl. the also-committed chatKnowledgePack). Stale `PARTNER_CATALOG_OFFERS`/`TIER_COUNTS` would serve customers wrong names/counts silently.
      _Fix:_ append `&& npm run build:partner-catalog` to BOTH `prebuild` and `predev`; delete the "deliberately NOT wired into prebuild" sentences from `docs/infrastructure/backend.md` and `docs/partner/igodirect-integration-playbook.md` §10; note in the script header that a legit CSV update must re-pin `EXPECTED_TOTAL`/`EXPECTED_CUMULATIVE` (fail-loud is the point).  _Shot:_ code-only  _Handled:_ 2026-07-24, working tree (uncommitted)
- [x] **F-003** · P1 · QA/Eng (convergence; folds in Arch guard) · `src/app/(site)/membership/page.tsx:44-72` + `MembershipPortalReturnBanner.tsx:95-136` — The untrusted-URL parser and the banner state matrix have zero tests and live in the wrong layers.
      _What:_ `resolvePortalReturn` (XSS strip, 60-char cap, ladder validation, precedence) is module-private in a page file; the 6-state matrix is inline in a hook-wired component this repo can't unit-test; `PortalReturn` type lives in a component that the server page imports. Also `PARTNER_CATALOG_OFFERS[offerId]` resolves prototype keys (`?offer_id=__proto__`) — safe today only by accident.
      _Fix:_ create `src/utils/partner-discounts/portal-return.ts` holding `PortalReturn`, `firstParam`, `resolvePortalReturn`, and a new pure `resolvePortalBannerView({acct, partnerAccessPct, portalReturn, ssoEnabled, recommended})` → `{headline, sub, cta}`; import from page + banner. Guard the id lookup with `/^\d+$/.test(offerId) && Object.hasOwn(...)`. IMPLEMENTATION NOTE: the offers map is DEPENDENCY-INJECTED into `resolvePortalReturn` (never imported by the util) — the client banner imports this module for its types/view resolver, so a direct map import would drag the 1,833-row file into the client bundle. Tests: 18 cases in `__tests__/portal-return.test.ts`, wired as `test:portal-return`.  _Shot:_ code-only  _Handled:_ 2026-07-24, working tree (uncommitted)
- [x] **F-004** · P1 · UX-C · `src/components/sections/membership/MembershipPortalReturnBanner.tsx:111-116, 128-131` — Expired-session members are told to buy a membership they already own; no login path in any guest state.
      _What:_ Portal sessions outlive our cookies; a lapsed-cookie member lands as `acct === "none"` and both guest states push purchase with no sign-in link.
      _Fix (as implemented — additive, approved sub copy kept):_ both guest states render an extra line `Already a member? <Link href="/login">Log in to check your access</Link>` under the sub (`showLoginHint` in the view resolver; pinned by `test:portal-return`).  _Shot:_ code-only  _Handled:_ 2026-07-24, working tree (uncommitted)
- [x] **F-005** · P1 · Orchestrator (demo-recording evidence) · banner `onSelectPlan` → `src/hooks/useMembershipCardCta.ts:156-159` — Active subscriber clicking "Unlock with the Boss" lands on the my-account dashboard with no upgrade context.
      _What:_ `cta.onSelect` routes existing subscribers to `/my-account` (dashboard home). The flagship persona (active member upgrading) must rediscover the Membership tab on their own.
      _Fix (as implemented — inside the banner, not MembershipPageClient):_ the banner's `handleUnlock` routes `acct === "active"` + subscription-plan recommendations to `/my-account?open=subscription` (ManageSheet — verified its tier taps open the upgrade/downgrade confirm, `ManageSheet.tsx:53`); everyone else falls through to `onSelectPlan` (modal + Klaviyo). Grids' `cta.onSelect` untouched.  _Shot:_ scratchpad `panel-artifact/ev-cta-routes-dashboard.webp` + `Downloads/rewards-return-phase1-demo.mp4` @~0:35  _Handled:_ 2026-07-24, working tree (uncommitted)

### Next

- [x] **F-006** · P1 · UX-C · `MembershipPortalReturnBanner.tsx:118-120` — Flag-off "You're set" state is a dead end contradicting the Rewards page's "Coming soon".
      _What:_ With `partnerDiscountSsoEnabled()` false, `cta = null`: told to "head back to the portal" with no path.
      _Fix:_ when `!ssoEnabled` in the covered state, sub → `"Head back to the partner portal tab you came from — this offer is ready to redeem."` (implemented in `resolvePortalBannerView`; pinned by `test:portal-return`)  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-007** · P1 · UX-C/Eng (merge) · `src/data/supportChatFaqs.ts:147-152` (id 16) + `:565-573` (id 72) — **LAUNCH GATE:** Cobber holds two contradictory redemption models; FAQ 72 ambiguous for pack holders and silent on Mini Packs.
      _What:_ FAQ 16 still says "mention Tools Australia when dealing with the partner brand — no code to enter"; FAQ 72 describes the portal. "Tradie unlocks 50%" is wrong for the Tradie Pack (40%).
      _Fix:_ FAQ 16 redemption sentence → the portal model (My Account → Rewards, signed in automatically); its `/partner` link REMOVED (that page is the B2B become-a-partner form — latent bug); "catalog"→"catalogue" within the edited answer. FAQ 72 disambiguated ("the Tradie membership unlocks 50%…") + Mini Pack slice named. Pack rebuilt, `test:chat-faqs` green, count stays 72. **Launch coupling:** this copy describes the portal-live world — ships in the same branch as the portal experience; the branch merge and the SSO flag flip must land together at vendor go-live.  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-008** · P2 · Arch/Eng (ruling: allowlist over delete) · `src/app/(site)/membership/page.tsx:70-77` — `offer_name` fallback reflects attacker-chosen text (incl. rule-11 vocabulary) into the trusted banner.
      _What:_ Any crafted `?offer_name=…&level=25` link renders up to 60 chars of arbitrary text as our headline. XSS is blocked; content spoofing is not.
      _Fix (as implemented, in `portal-return.ts` post-F-003):_ WeakMap-memoised name→offer index over the injected map; `offer_name` resolves only on a catalogue match (whitespace-collapsed + case-insensitive — vendor double-space names match); the CATALOGUE's name+pct always render; URL `level` ignored entirely; else `{generic:true}`. The 60-char display cap became moot (nothing user-typed renders). Known limitation: the 7 vendor punctuation-drift names ("JB HiFi" vs "JB Hi-Fi") miss the name allowlist → generic banner; `offer_id` templating stays the primary vendor ask.  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-009** · P2 · Eng/UX-C (merge) · `MembershipPortalReturnBanner.tsx:106-136` — Paused members see "You're at 0%" + a buy push instead of resume guidance.
      _What:_ No `paused` branch; `partnerAccessPct` is 0 for paused, so they're upsold a pack while their access returns on resume.
      _Fix:_ `paused` branch in `resolvePortalBannerView` (wins over offer states, like pastdue): "Your membership is paused." + "It resumes {date} — resume now to restore your discounts." (date-less fallback when pausedUntil unknown) + new `manage` CTA kind → `/my-account?open=subscription`; `pausedUntil: Date|null` from `useDashboardState` formatted en-AU in the banner. Pinned by `test:portal-return`.  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-010** · P2 · Eng · `src/generated/normToolsManifest.json` — Timestamp-only regeneration committed in a branch with zero Norm changes.
      _Fix (upgraded from per-commit hygiene to a STRUCTURAL fix, so it cannot recur for anyone):_ reverting the file before each commit only treated the symptom — `scripts/build-norm-manifest.ts` now compares the freshly-built endpoint set against the existing file and SKIPS the write when they match, so `generatedAt` means "when the manifest last actually changed" instead of "when someone last ran predev". Verified idempotent (two consecutive runs leave git clean) AND still rewriting when the endpoint set genuinely differs. Docs: `docs/internal-norm/backend.md`.  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree
- [x] **F-011** · P2 · UI-B · `MembershipPortalReturnBanner.tsx:181-183` — "Unlocks 1,833 of 1,833 partner offers" tautology.
      _Fix:_ `view.cta.cumulativeCount === PARTNER_CATALOG_TOTAL ? \`Unlocks all ${total} partner offers\` : …`  _Shot:_ scratchpad `reviewer-b/ev-meta-tautology-1280.webp`  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-012** · P2 · UI-B · `MembershipPortalReturnBanner.tsx:184-189` — "See all packages" tap target is 340×18px (44px minimum).
      _Fix:_ add `py-[13px] -my-1` to the link className. The `kind:"scroll"` anchor already measures 46px — leave it.  _Shot:_ scratchpad `reviewer-b/ev-seeall-tap-390.webp`  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-013** · P2 · UI-B · `src/app/(site)/membership/page.tsx:56` — 329 vendor names render as run-ons ("World Heritage Cruises Strahan TAS").
      _Fix (as implemented, post-F-003 locus):_ `displayName()` in `portal-return.ts` comma-joins `\s{2,}` on BOTH resolution paths (generated file stays vendor-faithful); pinned by `test:portal-return`.  _Shot:_ scratchpad `reviewer-b/ev-offer-name-runon-390.webp`  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-014** · P2 · UX-C · `src/hooks/queries/usePartnerDiscountSso.ts:32` + `src/app/api/partner-discount/sso/route.ts:34,60,83,91` — SSO failures print raw API-speak under a "partner portal" button.
      _Fix (exact strings, all implemented):_ hook fallback → `"Could not open the partner portal. Please try again."` · 502 → `"The partner portal is temporarily unavailable. Please try again shortly."` · 403 → `"Your partner access isn't active right now. You can check it on My Account → Rewards."` · 500 → `"Something went wrong opening the partner portal. Please try again."` · 404 (flag off) → `"The partner portal isn't available right now."` (status codes unchanged; guidance recorded in docs/partner/api.md + docs/client-state/gotchas.md)  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)
- [x] **F-015** · P2 · UX-C · `src/app/(site)/purchase-success/components/PurchaseSuccessClient.tsx:123-131` — "Back to the partner portal" shown to buyers who were never in it.
      _Fix:_ label → `"Open partner portal"` (matches banner + Rewards card).  _Shot:_ code-only  _Handled:_ 2026-07-28, working tree (uncommitted)

### Later

- [x] **F-016** · P2 · Eng · `page.tsx:42` + `unlock-packages.ts:49` + `build-partner-catalog-preview.ts:21` — Percent ladder defined in three lockstep copies.
      _Fix:_ export `PARTNER_CATALOG_LADDER_PCTS` from `src/utils/partner-discounts/unlock-packages.ts`; import in the other two; delete local copies. (If F-003 landed, put it in `portal-return.ts` instead.)  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-017** · P2 · Eng · `src/components/sections/dashboard/DashboardHero.tsx:24,48,104,109` + `my-account/page-client.tsx:317` — "Partner portal" button still wired through `onRewardPortal` prop.
      _Fix:_ rename prop to `onPartnerPortal` at declaration + call site; update the mention in `docs/shared-ui/frontend.md`.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-018** · P2 · Eng/QA/UI (merge) · `src/config/featureFlags.ts:45` + `src/hooks/useMembershipModalDeepLink.ts:20-22` — Two stale comments assert the pre-branch world.
      _Fix:_ featureFlags comment `"Reward portal"` → `"Partner portal"`; deep-link hook parenthetical → `"(both MembershipSection and /membership's MembershipPageClient wrap the open in whenGatesOpenElseGateModal)"`.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-019** · P2 · QA · `src/lib/queryKeys.ts` (absent) + `usePurchaseInvalidation.ts:27` + `usePartnerDiscountQueue.ts:74,86,109` — Fixed invalidation key is still a hand-copied literal.
      _Fix:_ add `partnerDiscounts: { queue: ["partnerDiscountQueue"] as const }` to queryKeys; import at all four sites.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-020** · P2 · QA · `src/utils/partner-discounts/__tests__/unlock-packages.test.ts:87-94` — Exact cheapest-package assertions missing for tiers 40/55/75/85.
      _Fix:_ add `testRequired40` (tradie-subscription + the 40% pack), `testRequired55` (foreman-pack), `testRequired75` (foreman-subscription at exact equality), `testRequired85` (the 85% pack); derive ids/prices from `src/data/membershipPackages.ts`.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-021** · P2 · Arch · `eslint/rules/no-models-in-client.js` — Server-only boundary of the 1,833-row map is a comment, not a guard.
      _Fix:_ add branch to `classify()`: `if (spec === "@/generated/partnerCatalogOffers") return "the server-only partner catalog map (bundle-size)";`; name the rule in the generated header comment.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-022** · P2 · UI-B (**DJ ruled: tighten it** — 2026-07-28) · `MembershipPortalReturnBanner.tsx:145` + `MembershipHero.tsx:90` — 86–106px header-height dead band between banner and hero.
      _Fix:_ `MembershipHero` takes `hasPortalBanner` (from `MembershipPageClient` as `Boolean(portalReturn)`); with the banner present it drops its own fixed-header offset for `pt-8 lg:pt-10`, since the banner already clears the header. Browser-MEASURED after the change: hero padding-top 32px @390 / 40px @1280 with the banner, and the no-banner control page still 86px / 106px (unchanged).  _Shot:_ scratchpad `reviewer-b/ev-hero-gap-390.webp` (before) · `panel-artifact/../f022-after-390.png` (after)  _Handled:_ 2026-07-28, working tree
- [x] **F-023** · P2 · QA · **re-opened 2026-07-28, CLOSED same day — the missing third part landed via F-030** · `e2e/specs/` — No e2e coverage of the banner, the legal-copy scan misses the param-gated copy, purchase CTA unasserted.
      _Fix:_ (1) new `e2e/specs/membership/portal-return-banner.spec.ts` `@smoke`: generic guest → "Unlock the partner catalogue"; `?offer_id=<real id>` → server-resolved name; XSS URL → generic + no pageerror; `?offer_name=Test Offer&level=50` → "Test Offer unlocks at 50% access."; bare `/membership` → banner absent; member states via storageState. (2) add `"/membership?utm_campaign=rewards-return"` to `legal-copy.spec.ts` PAGES (line 29). (3) append to `purchase-one-time.spec.ts` `@purchase`: flag-on → portal CTA visible post-webhook; flag-off → count 0.  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-024** · P2 · UX-C (merge; pre-existing debt) · `MembershipEntriesStack.tsx:125` + `PartnerPortalPhone.tsx:123` + `MiniDrawPackages.tsx:203,313` + `PartnerDiscountQueue.tsx:581` + `MiniDrawPackageModal.tsx:121` + `RewardsPartnerCard.tsx:122` — Portal naming/spelling sweep.
      _Fix:_ "rewards portal" strings → "partner portal" (×2 on /membership); "partner catalog" → "partner catalogue" (×4 rendered strings); "signed in via SSO" → "signed in automatically".  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd
- [x] **F-025** · P2 · UX-C · `MembershipPortalReturnBanner.tsx:130` — Guest generic sub implies all 1,833 offers come with any purchase.
      _Fix:_ sub → `` `Up to ${total} offers from Australia's top brands — your membership or one-time pack sets how much of the catalogue you unlock.` ``  _Shot:_ code-only  _Handled:_ 2026-07-28, commit 6e84bbfd

## Accepted deviations (recorded, no action)

- SSO button flag-gated even in the covered state — matches platform-wide dark launch; becomes spec-conformant when the flag flips at vendor go-live.
- Guest+offer state carries an unlock CTA the spec didn't enumerate — additive, consistent, kept.
- Vendor CSV contains merchant "Royal Casino Events Eltham VIC" (legit events-hire business) — its name would render verbatim via offer_id; awareness only, no banned-word scan hit (proper noun), revisit only if marketing objects.

---

# Verification panel — 2026-07-28 (round 2)

- **Reviewed:** `feature/igodirect-rewards-return` @ **`78c8cbbe`** (6 commits, clean tree). Base `origin/main` (3 commits ahead with unrelated draw-9 promo work; only BUSINESS.md overlaps, `git merge-tree` clean — merging is safe without re-merging first).
- **Purpose:** verify the 25 findings above are genuinely in the code, catch regressions the fixes introduced, and re-grade the 12 approved acceptance criteria.
- **Result:** **12/12 ACs still MET · 24/25 findings verified DONE · F-023 re-opened · 0 mis-claimed · zero scope creep.** Every "implemented differently than prescribed" finding (F-001, F-004, F-005, F-008, F-010, F-013, F-016, F-022) was traced at runtime and is coherent, not cosmetically ticked.
- **Artifact:** https://claude.ai/code/artifact/4559781c-da8f-4450-94af-a2cfa64fcfff
- **Gates:** type-check ✓ · lint **exactly** at the 6-error/33-warning baseline, with the flagged-file set proven disjoint from the 54 branch-touched files ✓ · `test:portal-return`, `test:unlock-packages`, `test:chat-faqs` (72), `test:member-level`, `test:member-status`, `test:sso-access` all ✓ · `build:partner-catalog` deterministic (identical MD5 across runs) ✓ · `build:norm-manifest` proven idempotent live ✓.
  **Lint caveat for future runs:** a bare `npm run lint` also walks the gitignored `e2e-artifacts/` Playwright report bundle and reports ~188 phantom errors from minified vendor JS. Always use `npx eslint . --ext .ts,.tsx,.js,.jsx --ignore-pattern "e2e-artifacts/**"`.
- **Evidence:** 24 state×viewport combinations at 390/1280, guest + freshly-authenticated member; every number from `getBoundingClientRect`/`getComputedStyle`/raw pixels. Crops in session scratchpad `reviewer-b2/evidence/`.
- **⚠ Two environment traps that invalidated evidence mid-review** (read before re-running): (1) the seeded `e2e-artifacts/.auth/member.json` goes **stale** on every `e2e:env` boot (the DB re-seeds with new ObjectIds) and silently degrades to a guest session — log in via `/login` and re-save, or your "member" results are guest results; (2) another worktree's dev server can seize port 3799 while the harness still reports "server ready" — see F-034.

## CORRECTION — a reported P0 was wrong

A reviewer reported as P0 that the banner's `#membership` CTA is inert because no element carries that id. **Disproven:** `src/components/sections/membership/MembershipTierChooser.tsx:151,158` renders `id={sectionId}` with `sectionId = "membership"` as its default, and `/membership` renders that component; the `@demo` spec that scrolls to `#membership` passes. The reviewer grepped only for the literal `id="membership"` and missed the prop-driven id. **No action — recorded so nobody re-opens it.**

## Findings (round 2)

### Now

- [x] **F-030** · P1 · Acceptance + QA (2-way convergence) · `e2e/specs/membership/purchase-one-time.spec.ts` — A ticked finding's fix is one-third unwritten, so the purchase-success portal CTA has zero coverage.
      _What:_ F-023's fix had three parts; (1) the banner @smoke spec and (2) the legal-copy URL landed, (3) the purchase-spec assertion was never written — yet F-023 was ticked with no override note. Both operands of `showPartnerPortalCta` (`status.processed` and the dark-launch flag) are untested; a false positive hands a buyer a portal button before access exists, a false negative hides the hand-off from a paying buyer.
      _Fix:_ inside the existing `@purchase` describe, after the benefits-granted assertion, wait for `/purchase-success`, then `const portalCta = page.getByRole("button", { name: "Open partner portal" })` and assert `toBeVisible()` when `process.env.PARTNER_DISCOUNT_SSO_ENABLED === "true"`, else `toHaveCount(0)`. NEVER click it (the SSO route POSTs the real iGoDirect tenant). Then re-tick F-023.  _Shot:_ code-only  _Handled:_ —
- [x] **F-028** · P1 · Eng + Arch (2-way convergence) · `src/utils/partner-discounts/portal-return.ts:180` — The "Already a member? Log in" hint shows to users who are already signed in, and the link ejects them from the funnel.
      _What:_ `showLoginHint` is keyed on `acct === "none"`, but `deriveDashboardAccountState` returns `"none"` for ANY authenticated user without an active membership or pack (registered-never-bought, expired pack, cancelled). `/login` redirects an authenticated session to `/my-account`, abandoning the offer context. This is a regression F-004 introduced — before it there was no link at all.
      _Fix:_ add `isAuthenticated: boolean` to `resolvePortalBannerView`'s input; set both guest branches to `showLoginHint: guest && !isAuthenticated`; pass `useSession().status === "authenticated"` from the banner. Add two cases to `test:portal-return`: `acct:"none", isAuthenticated:true` → `showLoginHint === false`; `false` → `true`.  _Shot:_ code-only  _Handled:_ —
- [x] **F-027** · P1 · UI (measured) · `src/components/sections/membership/MembershipPortalReturnBanner.tsx:105-113` — The anti-layout-shift skeleton is undersized; the hero still jumps up to 114.5px.
      _What:_ F-001 fixed the late mount (banner is in the DOM at first paint with `aria-busy` in 20/20 states) but the reserved height does not match the settled copy, so the page still shifts when the text swaps in — 114.5px on the flagship member+offer state at 390px (guest 68–70px; 1280 range 3–48px), landing 1.9–4.1s after DCL, i.e. inside the CLS window (~0.11, above the 0.1 "good" threshold). Only the control page measures 0.00.
      _Fix:_ size the shell to the tallest settled variant — headline block `h-[50px] sm:h-[56px]`, sub block `h-[62px]` — and mirror the CTA column's meta line + link blocks when `portalReturn.requiredPct != null`. Re-measure all 24 states; assert every shift is 8px or less.  _Shot:_ scratchpad `reviewer-b2/evidence/f001-skeleton-390.webp` + `f001-settled-390.webp`  _Handled:_ —
- [x] **F-031** · P1 · UX · `src/utils/partner-discounts/portal-return.ts:194-201` — A past-due member whose one-time pack is still live is told their discounts are off.
      _What:_ The past-due branch returns before any coverage check, so a member with a live Boss pack reads "Update payment to restore your discounts" — but the pack IS live and the portal would admit them. `useDashboardState:192-204` deliberately preserves `partnerAccessPct > 0` for this case and `RewardsPartnerCard:40` renders "Active from your pack", so the two surfaces contradict each other.
      _Fix:_ gate the branch as `if (acct === "pastdue" && partnerAccessPct === 0)` so a covered past-due member falls through to the correct covered/short-of state, and add a pack-covered past-due branch immediately after: headline "Your pack access is still running." / sub "Update payment to bring your membership discounts back on top of it." / `payment` CTA. Pin both in `test:portal-return`.  _Shot:_ code-only  _Handled:_ —
- [x] **F-029** · P1 · Arch (measured) · `src/utils/partner-discounts/portal-return.ts:63-74` — Six brands resolve to the wrong access percent, including telling a member they are covered when they are not.
      _What:_ The F-008 name index uses `Map.set`, so identical normalised names silently overwrite. 11 catalogue names collide and **6 carry different percents** — e.g. "Get Wines Direct" exists at both 100% and 50% and the index keeps 50%, so a 50% member is told "You're set — your 50% access covers Get Wines Direct" and is refused at the merchant. Inverse case: a 15% offer reported as needing 85%. Reproduced exactly against the committed catalogue. Live exposure is currently nil (only the `offer_name` path, which the vendor does not yet send).
      _Fix:_ build the index in two passes — collect keys whose entries disagree on `pct` into an `ambiguous` set and delete them, so ambiguous names degrade to the generic banner (the same graceful outcome as the known punctuation-drift names). Add `?offer_name=Get Wines Direct` → `{generic:true}` to `test:portal-return`.  _Shot:_ code-only  _Handled:_ —

### Next

- [ ] **F-032** · P1 · UX · `src/components/sections/membership/PartnerPortalPhone.tsx:50` — The vendor's brand name "My Rewards" is printed on `/membership`, directly above a button reading "Open partner portal". The F-024 sweep edited line 123 of this same file and missed line 50.
      _Fix:_ change the mock's title to `Partner portal`.  _Shot:_ code-only  _Handled:_ —
- [ ] **F-033** · P1 · UX · `RewardsPartnerCard.tsx:110-119` + `src/app/(site)/my-account/page-client.tsx:81,317` — Two of the four portal buttons swallow every SSO error silently.
      _What:_ Both call `sso.mutate()` but never read `sso.error`, so F-014's four strings never reach members on the Rewards page — the exact surface Cobber's FAQ 16 sends them to. The label flicks to "Opening…" and back with no explanation.
      _Fix:_ render `sso.error.message` with `role="alert"` under the Rewards card's SSO button; surface `partnerSso.error?.message` as a toast from the dashboard chip.  _Shot:_ code-only  _Handled:_ —
- [ ] **F-034** · P1 · QA tooling · `e2e/run.ts:297,313` + `e2e/lib/health.ts:10-14` — The harness reports "server ready" when a different app owns the port.
      _What:_ With port 3799 held by another worktree, `next dev` dies with `EADDRINUSE` but the `npm run dev` wrapper survives, so the child-exit guard never fires and the health check is satisfied by the squatter — the review then runs against another branch's code. This produced roughly 15 minutes of phantom results in this panel (banner absent, pre-branch strings) that were nearly reported as regressions. `assertPortFree` cannot cover it — the squatter can arrive after the check.
      _Fix:_ after `waitForHttpOk` in step 4, read `e2e-artifacts/logs/server.log` and throw if it contains `EADDRINUSE`, naming the port and the `netstat` command to find the owner.  _Shot:_ scratchpad `reviewer-b2/env4.log`, `env5.log`  _Handled:_ —
- [ ] **F-035** · P2 · UI (measured) · `MembershipPortalReturnBanner.tsx:44` + `RewardsPartnerCard.tsx:93` — The primary CTA label fails WCAG AA on the light end of its gradient.
      _What:_ White 13.5px/900 on `from-[#ff5a5a]` measures 4.46–4.49:1 averaged and **3.06–3.13:1 at the lightest pixel**; 4.5:1 is required (13.5px bold is not "large text"). Affects every state's primary action at both widths.
      _Fix:_ change `from-[#ff5a5a]` to `from-[#e02424]` in both files (white on `#e02424` = 4.72:1; the `to-[#c40d0d]` end is already 6.15:1). Re-sample and assert the worst-pixel ratio is at least 4.5.  _Shot:_ scratchpad `reviewer-b2/evidence/f011-f012-meta-cta-390.webp`  _Handled:_ —
- [ ] **F-050** · P2 · Docs (merged drift set; 3 reviewers) — Seven docs assert things this branch changed.
      _Fix:_ (a) **`docs/partner/igodirect-integration-playbook.md:263,371` — highest value, it is the row we hand iGoDirect:** replace the `offer_name`/`level` contract with "`offer_name` resolves ONLY on an exact catalogue-name match (case/whitespace-insensitive); URL `level` is ignored entirely — `offer_id` is the reliable templating key"; add `paused` to the state-matrix list; rename the CTA to "Open partner portal"; fix vendor ask #1 the same way. (b) `CUSTOMER.md:425` — "Back to the partner portal" becomes "Open partner portal". (c) `docs/dashboard-account/frontend.md:190,540` + `docs/config-and-data/architecture.md:25` — "Reward portal" becomes "Partner portal". (d) `docs/shared-ui/frontend.md` — add the paused row to the state-matrix table, and fix the "Layout note" (plus the banner's own JSDoc at `MembershipPortalReturnBanner.tsx:58-60`) which still says the hero keeps its own offset; F-022 changed that. (e) `docs/internal-norm/backend.md:27` — the script does NOT call `zod-to-json-schema`; it emits `registryKey/tier/path/method/summary` only and the manifest publishes no schemas. (f) `scripts/build-partner-catalog-preview.ts:153` — emit the parsed `total` rather than a hard-coded 1,833 and name the `local/no-models-in-client` rule in the generated header (F-021's second half).  _Shot:_ code-only  _Handled:_ —

### Later / own PR

- [ ] **F-026** · P1 · A11y (site-wide, pre-existing) · `src/app/globals.css:1124-1127,1131-1139` — Keyboard focus is invisible on every control; proven by a 0-pixel diff between focused and unfocused screenshots (0 of 105,984 pixels differ).
      _What:_ `*:focus { outline:none !important }` and `button:focus-visible { outline:none !important }` override every `focus-visible:ring-*` utility, so the focus styling on this branch's five new controls is dead code, and the two link-style controls lose the UA default ring too. WCAG 2.4.7 fail.
      _Fix:_ narrow the reset to `*:focus:not(:focus-visible)`, drop `button:focus-visible` from it, and add focus classes to the "See all packages" anchor (`:200`) and the `/login` hint (`:171`). Assert the pixel diff is non-zero. **Own PR:** globals.css affects every page and needs its own regression sweep.  _Shot:_ scratchpad `reviewer-b2/evidence/focus-unfocused-390.webp` + `focus-focused-390.webp`  _Handled:_ —
- [x] **F-036** · P2 · QA · `src/utils/partner-discounts/__tests__/portal-return.test.ts` — The covered/short-of boundary is untested; changing `>=` to `>` at `portal-return.ts:225` leaves all 18 cases green. **183 offers sit at exactly 50%**, the entry subscription tier — the highest-traffic member/offer pairing on the surface.
      _Fix:_ add `testViewCoveredAtExactEquality` asserting 50-vs-50 gives "You're set" + the `sso` CTA, and 50-vs-55 gives the short-of headline + the `unlock` CTA.  _Handled:_ 2026-07-28, working tree — folded into the F-031 edit (same function, same test file).
- [ ] **F-037** · P2 · QA (2-way convergence) · `e2e/specs/marketing/legal-copy.spec.ts:42-44` — The scan can read the banner's skeleton and pass having scanned no banner copy; it never goes red, so nobody notices.
      _Fix:_ for the rewards-return URL, wait for `aria-busy` to clear on the banner section AND assert the Amazon headline is visible before reading `body.innerText()`.  _Handled:_ —
- [ ] **F-038** · P2 · Arch/QA (2-way convergence) · `eslint/rules/no-models-in-client.js:31` — The bundle guard cannot fire on the files that actually carry the risk: it returns early for any file without `"use client"`, but the documented risk is the client-SHARED utils (`portal-return.ts`, `unlock-packages.ts`, `partner-catalog-visibility.ts`), none of which carry that directive.
      _Fix:_ either add a path-scoped `no-restricted-imports` block for `@/generated/partnerCatalogOffers` in `eslint.config.mjs` (excluding `src/app/**/page.tsx`), or extend the rule's early return with a CLIENT_SHARED path regex. Add a `RuleTester` test asserting: a `"use client"` file importing it reports; a client-shared util importing it reports; `import type` stays silent.  _Handled:_ —
- [ ] **F-039** · P2 · Eng · `scripts/build-partner-catalog-preview.ts` — 1,833 vendor names render verbatim as our headline and nothing scans a refreshed CSV for rule-11 vocabulary. Six merchants already carry casino/lucky wording (e.g. `1012029` "Royal Casino Events", `1068133` "B. Lucky & Sons"); a future CSV containing "Sportsbet"/"Lottoland"/"TAB"/"Keno" would ship a hard-banned word into customer copy with nothing failing.
      _Fix:_ after the row loop, `fail()` on `/\b(odds|lotter(y|ies)|lotto|raffles?|sweepstakes?|gambl(e|ing)|bets?|betting|wager|keno|pokies)\b/i` listing the offending ids (a legitimate merchant then becomes an explicit reviewed allowlist entry), and `console.warn` the soft set `/\b(lucky|casino|jackpot)\b/i`. Update the accepted-deviations note to list all six, not one.  _Handled:_ —
- [ ] **F-040** · P2 · QA · `src/utils/partner-discounts/__tests__/unlock-packages.test.ts:85-91` — The record claims "all 11 tiers pinned"; tiers **10 and 15 are invariant-only** (367 offers). `testMiniTierPercents`'s docstring says 5/10/15 but its body only exercises 5.
      _Fix:_ loop the body over `[5,10,15]` asserting `tradie-subscription`/$20 and `apprentice-pack`/$25 for each; the "all 11" claim then becomes true.  _Handled:_ —
- [ ] **F-041** · P2 · QA · `scripts/build-partner-catalog-preview.ts:33-34` — Two drift gaps: `EXPECTED_CUMULATIVE` pins 3 of 11 tiers (moving offers between unpinned adjacent tiers passes the fail-loud check while the generated counts shift), and nothing asserts the COMMITTED generated files still match the committed CSV, so a CSV-only commit passes every local gate.
      _Fix:_ pin all 11 cumulatives; add `src/utils/partner-discounts/__tests__/partner-catalog-drift.test.ts` (wired as `test:partner-catalog-drift`) that re-parses the CSV and asserts row count, map size and every cumulative agree with the committed constants.  _Handled:_ —
- [ ] **F-042** · P2 · QA · `e2e/specs/membership/portal-return-demo.spec.ts:39-41,77-79` — The `@demo` spec has the identical cold-server dependency the `@smoke` spec was hardened against but received neither mitigation — and a tighter 30s budget. A timeout mid-narration burns a proof-mode take.
      _Fix:_ hoist the same `test.describe.configure({ timeout: 150_000 })` plus the real-navigation `beforeAll` warm-up, and raise both headline timeouts to 60s.  _Handled:_ —
- [ ] **F-043** · P2 · QA · `e2e/specs/membership/portal-return-banner.spec.ts:44-48` — `bannerSettled`'s docstring claims it covers the skeleton; `not.toHaveAttribute` also passes when the attribute is ABSENT, so a regression to `return null` (the 238–364px CLS bug) would pass silently. The skeleton has no assertion anywhere.
      _Fix:_ add a test that navigates with `waitUntil:"commit"` and asserts `aria-busy="true"` is present BEFORE asserting it clears; correct the docstring.  _Handled:_ —
- [ ] **F-044** · P2 · QA · `src/app/api/partner-discount/sso/route.ts:37,52,67,91,100` + `usePartnerDiscountSso.ts:34` — The five customer-facing SSO error strings have no coverage in any layer; a refactor reverting one to "Forbidden" is invisible to every gate. The 403 string is load-bearing (it is why a 403 does not sign the user out).
      _Fix:_ extract them to an exported `PARTNER_SSO_ERRORS` const in `sso-access.ts`, reference it from the route and the hook, and extend `test:sso-access` with a check that no message matches API-speak/status-code/banned-copy patterns and that the 403 string names "My Account → Rewards".  _Handled:_ —
- [ ] **F-045** · P2 · Arch · `scripts/build-norm-manifest.ts:35-42` — The write-guard compares 2 of the file's 3 top-level fields. Correct today (the manifest has only `version`, `endpoints` and the timestamp), fragile the moment a field is added.
      _Fix:_ destructure `generatedAt` off both objects and compare the rest, so the guard stays correct by construction.  _Handled:_ —
- [ ] **F-046** · P2 · Eng/QA (2-way convergence) · `MembershipPortalReturnBanner.tsx:121` — `acct as PortalBannerAcct` is a no-op cast that disables the only compile-time link between the two state unions; a future 6th dashboard state would compile silently and fall into the generic arm — exactly the class of bug F-009 was filed for.
      _Fix:_ replace the hand-duplicated union at `portal-return.ts:118` with `export type PortalBannerAcct = DashboardAccountState;` (type-only import, erased) and delete the cast.  _Handled:_ —
- [ ] **F-047** · P2 · Eng · `MembershipPortalReturnBanner.tsx:97` — Signed-out visitors are held behind `/api/major-draw`, a query the banner never reads. Their state is fully resolved server-side, yet they wait 3–8s (longer under retry backoff) for the funnel's primary landing copy.
      _Fix:_ gate the skeleton as `if (isLoading && sessionStatus !== "unauthenticated")` so guests paint resolved copy on first render; note it in the F-001 paragraph of `docs/shared-ui/frontend.md`.  _Handled:_ —
- [ ] **F-048** · P2 · UX (copy set) — Four nits: (a) the paused and past-due states discard `offerName`, so the member cannot tell whether fixing billing unlocks the offer they came for; (b) the paused sub says "resume now" but the button says "Manage membership" (ManageSheet renders a real "Resume now" control); (c) `supportChatFaqs.ts:570` says "access level", a third name for "partner access" (19 hits site-wide); (d) the 403 string can contradict the banner directly above it ("You're set…" then "not active right now") and, once F-033 lands, tells Rewards-page users to go to the page they are already on.
      _Fix:_ (a) append ", then check {offerName} again" when known; (b) label the CTA "Resume membership"; (c) "…your partner access doesn't reach it yet."; (d) "Your partner access isn't active right now — it may have just expired. Refresh and try again, or contact us if it looks wrong."  _Handled:_ —
- [ ] **F-049** · P2 · UX · `supportChatFaqs.ts:61,242` + `scripts/build-chat-knowledge-pack.ts:74,83,168,176,187` — Cobber says "catalogue" and "catalog" in the same conversation; the FAQ 16/72 rewrite made the generated pack self-contradicting (11 `catalog` vs 6 `catalogue`). Every rendered UI string is already British.
      _Fix:_ change those seven lines to "catalogue", rebuild the pack, re-run `test:chat-faqs`.  _Handled:_ —

## Accepted deviations (round 2)

- **F-025 changed an owner-approved string.** The generic-guest sub was rewritten for accuracy (the approved copy implied all 1,833 offers came with any purchase; an Apprentice Pack opens 459). Disclosed at the time, and DJ ran the batch containing it — worth a one-line confirm, not a blocker.
- **Two states were added post-approval** (the paused branch, and the guest login hint), both recorded when they landed.
- **Six merchants, not one, carry casino/lucky vocabulary** in the vendor catalogue (superseding the round-1 note that named only "Royal Casino Events"). None trips a banned-word scan as a proper noun; F-039 adds the build-time guard for a future CSV refresh.
