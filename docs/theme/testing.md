# Theme — Testing

## Manual smoke

- Toggle theme → verify class on `<html>` updates
- Refresh → verify theme persists (no flash)
- Auto-theme at scheduled hour boundary → verify auto-switch
- Promo route → verify promo theme override; navigate away → verify clean revert
- Admin panel → verify admin theme is independent
