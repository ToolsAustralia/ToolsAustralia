# Theme — Backend

_N/A — theme is pure client-state. No server-side persistence beyond a possible cookie for cross-page-load consistency._

> _TODO: confirm whether theme preference is stored server-side on `User` or only in cookie/localStorage._
