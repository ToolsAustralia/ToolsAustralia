# Settings Redesign Phase 2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Frontend-only; NO UI test runner — verify with `npx tsc --noEmit` (no NEW errors beyond ~9 pre-existing `.webp`) + `npx eslint <files>` clean + acceptance checklist. **One commit per phase**, no push. Commit body ends `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`; gpg error → `git -c commit.gpgsign=false commit`. If the doc-sync Stop hook flags a domain doc stale on commit, re-stage the relevant `docs/<domain>/*.md` (Phase E finalizes docs) so the commit passes — don't expand doc scope mid-phase.

**Goal:** Merge the Claude design into the real Subscription & Payment settings tabs (functionality 100% preserved) + four polish items, without regressing any modal/SettingsModal usage.

**Architecture:** Opt-in `settingsRedesign` prop on `SubscriptionManagementModal` and `PaymentMethodsTab`, passed ONLY by the settings tab wrappers. New presentational view files receive all data/handlers as props from the unchanged orchestrators and reuse the exact same pure calc utilities. Modal-mode and SettingsModal render paths stay byte-identical.

**Spec:** `docs/superpowers/specs/2026-05-19-settings-redesign-phase2-design.md`. **Design JSX (read-only):** `c:\tmp\design_extract\tools-australia\project\settings\{subscription-tab,payment-tab,password-tab}.jsx`. **Phase-1 primitives:** `src/app/(site)/my-account/components/settings/ui/primitives.tsx` (Card, SectionHeader, Field, SettingsInput, SettingsButton, SettingsBadge). Tokens `shadow-lift dark:shadow-lift-dark`, `animate-pulse-ring`, `font-poppins`. lucide icons. `cn` from `@/utils/cn`.

**Global rules:** No change to `src/app/api/**`, `src/services/**`, `src/lib/**`, `src/models/**`, existing hooks, any endpoint/mutation/localStorage/`window.location.reload()` behavior, the modal-mode render, or the SettingsModal embed. No `any`. No new dependency. No new file unless this plan creates it.

---

### Task A: Polish — footer, rename, spacing, password security score

**Files:** Modify `src/app/(site)/my-account/settings/page.tsx`, `src/app/(site)/my-account/components/settings/SettingsSidebar.tsx`, `src/app/(site)/my-account/components/settings/PasswordTab.tsx`.

- [ ] **Step 1 — Remove member-since footer.** In `page.tsx` delete the footer `<p>…Member since…</p>` block in the index view. Remove any now-unused `createdAt` formatting introduced solely for it.
- [ ] **Step 2 — Rename Profile → Account details.** In `SettingsSidebar.tsx` `SETTINGS_TABS`, the `profile` entry: `label: "Account details"`, `shortLabel: "Account"`. **Keep `id: "profile"`** (URL `?tab=profile`, `VALID_TAB_IDS`, deep links unchanged). Verify `page.tsx` derives the `DashboardHeader` title and the index preview-card heading from `SETTINGS_TABS` label (no hard-coded "Profile" string remains for that tab; grep `"Profile"` in `page.tsx`/`SettingsSidebar.tsx` and fix any literal that should be the label). Do not touch the bottom-nav.
- [ ] **Step 3 — Spacing between sticky tab strip and content.** In `page.tsx` tab-view: the mobile sticky segmented strip and the content must have clear separation. Add `pt-6 sm:pt-8` (or equivalent ≥24px) to the tab-content container so the first `SectionHeader` is not flush against the strip; keep desktop layout visually balanced (sidebar + content). Verify at ~360px no overlap and on desktop no excessive gap.
- [ ] **Step 4 — Password security score (frontend, deterministic).** In `page.tsx`, extend `<PasswordTab>` props additively: pass `isEmailVerified={user.isEmailVerified}` and `hasPassword={user.hasPassword}`. In `PasswordTab.tsx`:
  - Extend `PasswordTabProps` additively: `isEmailVerified?: boolean; hasPassword?: boolean;`.
  - Add a pure local `computeSecurityScore({ hasPassword, isEmailVerified, newPasswordStrength }): { score: number; label: "Strong"|"Decent"|"At risk" }` — weights: password set (`hasPassword !== false`) = 35, email verified (`isEmailVerified === true`) = 35, 2FA = 0 (always unmet — "Coming soon"); plus, when `newPassword` non-empty, add `Math.round((calculatePasswordStrength(newPassword)/4)*30)` capped so total ≤ 100; when no new password, the remaining 30 is unmet. Label: `>=80 Strong`, `>=60 Decent`, else `At risk`.
  - Render the design's `ScoreDial` (port the SVG radial gauge from `password-tab.jsx` `ScoreDial`, tier color from label: emerald/amber/red) + a truthful checklist: "Password set" (met iff `hasPassword !== false`), "Email verified" (met iff `isEmailVerified`), "Two-factor authentication" (unmet, label "Coming soon"), "Strong new password" (met iff `newPassword && calculatePasswordStrength>=3`, neutral when no new password). Place this as the security-overview hero `Card` at the top of the tab (above "Change password"). NO fabricated/server numbers; everything derives from the two real props + the live new-password value.
  - Keep ALL existing handlers/validation/toasts/strength-bar/requirements-panel/SMS/email-recovery byte-identical.
- [ ] **Step 5 — Verify:** `npx tsc --noEmit`; `npx eslint "src/app/(site)/my-account/settings/page.tsx" "src/app/(site)/my-account/components/settings/SettingsSidebar.tsx" "src/app/(site)/my-account/components/settings/PasswordTab.tsx"`.
- [ ] **Step 6 — Acceptance:** no member-since footer; tab labelled "Account details"/"Account" everywhere (id still `profile`, deep link works); visible gap between strip and first header at mobile & desktop; ScoreDial + checklist render with deterministic values across states (verified/unverified email, with/without new password) and update live as the new password is typed; light+dark OK; all password actions unchanged.
- [ ] **Step 7 — Commit:** `git add` the 3 files (+ any doc-sync-required `docs/dashboard-account/*`); `git -c commit.gpgsign=false commit -m "feat(settings): polish — drop member-since, Profile→Account details, tab spacing, frontend password security score" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

---

### Task B: Index payment preview brand/last4 from Stripe

**Files:** Modify `src/app/(site)/my-account/settings/page.tsx`.

- [ ] **Step 1 — Wire the existing hook.** Import and call `useSavedPaymentMethods` (`@/hooks/useSavedPaymentMethods` — it resolves `userId` internally via `useUserContext`; confirm export name by reading the file). Use its returned `paymentMethods` (`SavedPaymentMethod[]` with optional `card?: { brand; last4; expMonth; expYear }`), `subscriptionDefaultPaymentMethodId`, and `loading`. Call it unconditionally at the top of the component with the other hooks (no conditional hook).
- [ ] **Step 2 — Compute the preview.** For the payment preview card summary: pick the default method (`isDefault` or matching `subscriptionDefaultPaymentMethodId`, else first). If it has `card`, primary = `` `${titleCase(card.brand)} •••• ${card.last4}` `` and secondary = `"Default"` when it is the default; if methods exist but card meta not yet loaded (`loading` or no `card`) fall back to the Phase-1 count text (`"<n> card(s) saved"` + "Default set"); if none → `"No cards saved"`. `titleCase` = capitalize first letter (`brand.charAt(0).toUpperCase()+brand.slice(1)`). Guard every `card?`/array optional; never throw while loading.
- [ ] **Step 3 — Verify:** `npx tsc --noEmit`; `npx eslint "src/app/(site)/my-account/settings/page.tsx"`.
- [ ] **Step 4 — Acceptance:** index payment card shows e.g. "Visa •••• 4242" + "Default" when a default card exists; gracefully shows count/loading text otherwise; no crash when Stripe data loading or absent; no new endpoint added; no other preview changed.
- [ ] **Step 5 — Commit:** `git add` page.tsx (+ doc-sync docs if required); `commit -m "feat(settings): index payment preview shows real card brand/last4 via useSavedPaymentMethods" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

---

### Task C: Subscription tab merge (settings-variant re-skin)

**Files:** Modify `src/components/modals/SubscriptionManagementModal/index.tsx`, `src/components/modals/SubscriptionManagementModal/types.ts` (props), `src/app/(site)/my-account/components/settings/SubscriptionTab.tsx`. Create `src/components/modals/SubscriptionManagementModal/SettingsRedesignSubscription.tsx`.

- [ ] **Step 1 — Add the opt-in prop.** In `types.ts`/`SubscriptionManagementModalProps` add `settingsRedesign?: boolean`. In `SubscriptionTab.tsx` pass `settingsRedesign` alongside the existing `renderAsPanel` (keep all other props identical). `SettingsModal` and `MembershipStatus` unchanged → they never set it.
- [ ] **Step 2 — Hoist the 5 child modals.** In `index.tsx`, the `UpgradeConfirmModal`/`DowngradeConfirmModal`/`StripePaymentModal`/`CancellationFlowModal`/`RenewalFailedModal` currently live inside the `subscriptionContent` fragment. Refactor so they render **once, unconditionally**, regardless of the legacy/settings branch (e.g. wrap: `<>{bodyBranch}{the five child modals}</>`), with byte-identical props/state wiring. They must NOT be duplicated and must work in both branches.
- [ ] **Step 3 — Branch the main body only.** Where `subscriptionContent`'s main block is built (the state selector ~836-865 inside `<ModalContent>`): when `renderAsPanel && settingsRedesign`, render `<SettingsRedesignSubscription {...} />` (props below); otherwise the legacy block verbatim. Keep `{!renderAsPanel && <ModalHeader/>}` and the structural panel/modal fork (~997-1005) exactly as-is for non-settings callers. The settings branch returns the bare `<div className="w-full">` wrapper like the existing panel path.
- [ ] **Step 4 — Create `SettingsRedesignSubscription.tsx`** (presentational; props only; no hooks/fetch/business logic). Port the Claude `subscription-tab.jsx` look using Phase-1 primitives + tier theming via `getMembershipSectionColorScheme(planId, true)` (`@/utils/package-colors/packageColorScheme`). Props it receives from `index.tsx` (all already-derived there): the resolved `membershipPackage`, `activeSubscription`, `subscriptionBenefits`, `hasFailed`, `subscriptionBenefits.isCancelled`, `availableUpgrades`, `availableDowngrades`, `pendingBenefitCountdownProps`, the existing state setters/handlers (`onSelectUpgrade`=set+`setShowUpgradeConfirm(true)`, `onSelectDowngrade`=set+`setShowDowngradeConfirm(true)`, `onCancel`=`handleCancelSubscription`, `onResume`=`handleReactivateSubscription`, `onResolveFailed`=`setIsRenewalFailedModalOpen(true)`, `onSubscribeClick` for empty states), `useMemberships()`-derived available packages (for guest plan cards/feature matrix), and any already-computed display values that are NOT pure utils (e.g. `lastMonthAccumulatedEntries`, `cancellationEntrySnapshot`, discounted price) — pass these DOWN; do not recompute.
  - For numbers that ARE pure utils, import and call the SAME functions with the same inputs: `calculateRenewalEntries`/`calculateUpgradeEntries` from `@/utils/payment/subscription-entries-calculator`, `getPartnerAccessDurationLabel` from `@/utils/partner-discounts/partner-access-duration`. Discount price = `price * (1 - (percentOff ?? 0)/100)` (same formula as `CurrentBenefitsCard`). Do NOT re-implement any logic differently; same fn, same inputs ⇒ identical output.
  - State → design mapping per spec §5: Active/past-due-autorenew → PlanHero (tier-themed) + benefits + (failed → PastDueRecovery wired to `onResolveFailed`/`onCancel`; pending → `PendingChangeBanner`/`BenefitCountdown` reused) + (if `!hasFailed`) Manage section: design `ManageRow`s for each `availableUpgrades`/`availableDowngrades` entry (selection → the passed callbacks that open the real confirm modals), auto-renew row display-only, cancel/resume row → `onCancel`/`onResume`. TierLadder from the real packages (presentational only). Plan benefits from `subscriptionBenefits.currentBenefits`/package features. One-time-only/inactive/no-sub → re-skinned empty states (callbacks unchanged). Guest → design plan-cards + feature-matrix from the real `useMemberships()` packages; "Choose <tier>" → `onSubscribeClick` path.
  - **OMIT + flag** the design's `AccumulationChart` (no real entry-history data — codebase never fabricates it). Render `BillingCalendar` as a single real "Next bill" card from the real next-billing date only (no synthetic future cycles).
  - Every element light+dark; tier gradients via the existing color-scheme util (inline style is acceptable here — that is the codebase's established tier-theming mechanism).
- [ ] **Step 5 — Verify modal-mode untouched.** Confirm via reading: `MembershipStatus.tsx` and `SettingsModal` render paths produce the SAME JSX as before (they don't pass `settingsRedesign`; the only diff in `index.tsx` is the hoisted child modals — assert their props/state are identical and they still render for the legacy branch). `npx tsc --noEmit`; `npx eslint` the 4 touched/created files.
- [ ] **Step 6 — Acceptance:** settings Subscription tab matches the Claude design (minus AccumulationChart + synthetic cycles) across member(tradie/foreman/boss)/past_due/one-time/inactive/guest; every action (upgrade→confirm modal, downgrade→confirm modal, cancel→cancellation flow, resume, resolve-failed→renewal modal, subscribe) works through the original handlers; numbers (entries/discount/partner-duration) identical to legacy; `SettingsModal` + `MembershipStatus` modal render unchanged; localStorage/reload behavior intact; light+dark; no horizontal scroll ~360px.
- [ ] **Step 7 — Commit:** `git add` the 4 files (+ doc-sync docs if required); `commit -m "feat(settings): merge Claude design into Subscription tab (settingsRedesign variant; logic preserved)" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

---

### Task D: Payment tab merge (settings-variant re-skin)

**Files:** Modify `src/components/modals/PaymentMethodsTab/index.tsx`, `src/app/(site)/my-account/components/settings/PaymentTab.tsx`. Create `src/components/modals/PaymentMethodsTab/SettingsRedesignPayment.tsx`.

- [ ] **Step 1 — Add the opt-in prop.** Add `settingsRedesign?: boolean` to `PaymentMethodsTab`'s props. `PaymentTab.tsx` passes `settingsRedesign` (keep the existing trimmed `user` object prop identical). `SettingsModal` caller unchanged → never sets it.
- [ ] **Step 2 — Branch the render in `index.tsx`.** Keep ALL hooks/handlers/state/`stripePromise` singleton/`Elements`/`ConfirmationModal` delete-flow in `index.tsx` unchanged. When `settingsRedesign`, render `<SettingsRedesignPayment {...presentationalProps} />` instead of the legacy `<div>` tree; otherwise legacy verbatim. The `ConfirmationModal` (delete-flow) and `Elements`+`AddPaymentForm` add path must render in BOTH branches with identical wiring — hoist them so they are not duplicated and not gated away (mirror Task C Step 2 approach: branch only the list/empty/banner body; keep modals/Stripe-add unconditionally wired).
- [ ] **Step 3 — Create `SettingsRedesignPayment.tsx`** (presentational; props only). Port the Claude `payment-tab.jsx` look using Phase-1 primitives: wallet grid of realistic credit-card visuals using REAL `paymentMethod.card.{brand,last4,expMonth,expYear}`; default card gets the ring/badge; failed/past-due banner (from the existing `hasActiveSubscription`/error/`isPastDue`-equivalent props passed down); dashed Add-card slot → triggers the existing `handleAddNewPaymentMethod`; per-card Set-default/Remove → existing `handleSetDefault`/`handleDeleteClick` passed as props; loading/error/empty states re-skinned. Map `card.brand` → a styled brand mark (frontend presentational: e.g. "VISA"/Mastercard circles/"AMEX" per the design's `CreditCard` skins) replacing the placeholder 💳; unknown brand → generic. The Stripe `AddPaymentForm` is reused as-is (chrome wrapper may be restyled but its props/`confirmSetup`/billing-address logic unchanged). Stripe security note + privacy link retained. Every element light+dark.
- [ ] **Step 4 — Verify SettingsModal untouched.** Confirm the non-`settingsRedesign` path is byte-identical (only addition: hoisted modal/add-form if refactored — assert identical props/state). `npx tsc --noEmit`; `npx eslint` the 3 touched/created files.
- [ ] **Step 5 — Acceptance:** settings Payment tab matches the Claude design across has-cards/empty/add-form/loading/error/past-due; add card (Stripe), set default, delete (incl. `billing-last` checkbox confirm), default-card subscription reassignment all work via the original handlers; real brand/last4 shown; `SettingsModal` payment usage unchanged; light+dark; no horizontal scroll ~360px.
- [ ] **Step 6 — Commit:** `git add` the 3 files (+ doc-sync docs if required); `commit -m "feat(settings): merge Claude design into Payment tab (settingsRedesign variant; Stripe logic preserved)" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

---

### Task E: Docs + flag list + final comprehensive review

**Files:** Modify `docs/dashboard-account/frontend.md`, `docs/subscription/` and `docs/payment/` (whatever the doc-sync hook maps for the touched modal/hook paths).

- [ ] **Step 1 — Update domain docs.** Document: the `settingsRedesign` opt-in prop on `SubscriptionManagementModal` & `PaymentMethodsTab`; the two new presentational view files; that modal/SettingsModal paths are byte-identical; the Phase-A polish; the frontend password security score (deterministic, inputs); the index payment-brand wiring. Match existing doc style; don't rewrite unrelated content. Update the "Flagged / deferred" list so it now contains ONLY: AccumulationChart (no entry-history data), the 3 synthetic future billing cycles (only real next-bill shown), SMS 2FA (Coming soon). Remove items now implemented (Sub/Pay deep redesign, password score, index brand, member-since, profile rename) and note them as done.
- [ ] **Step 2 — Verify:** `npx tsc --noEmit` clean (no new errors); `npm run lint` (no NEW errors from touched files).
- [ ] **Step 3 — Final comprehensive review** (dispatch a fresh Opus reviewer over `git diff <phase2-base>..HEAD`): assert (a) zero backend/hook/service/model/endpoint change; (b) modal-mode `SubscriptionManagementModal` (MembershipStatus, dev gallery) and `SettingsModal` embeds byte-identical; (c) numbers (entries/discount/partner-duration) identical to legacy via the same utils/passed values; (d) all flows work through original handlers; (e) no fabricated data (AccumulationChart/synthetic cycles absent); (f) build clean for changed files; (g) design conformance; (h) flag list accurate. Produce the consolidated report.
- [ ] **Step 4 — Commit:** `git add docs/`; `commit -m "docs(settings): Phase-2 docs + updated flag list (Sub/Pay merge, password score, index brand)" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

---

## Spec coverage self-check
- Spec §3 polish → Task A. §4 password score → Task A Step 4. §7 index brand → Task B. §5 Subscription merge (+gating, child-modal hoist, util reuse, omit AccumulationChart/synthetic cycles) → Task C. §6 Payment merge (+gating, Stripe preserved) → Task D. §2 premise/no-regression → Tasks C/D gating + global rules + Task C Step 5 / D Step 4 + Task E final review. §8 docs/flags → Task E. ✓
- No-fabrication constraint → Task A Step 4 (deterministic), Task C Step 4 (omit AccumulationChart, real next-bill only). ✓
- Modal-mode byte-identity → explicit verification steps in C5/D4 + final review E3. ✓
